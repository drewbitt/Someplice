# Path to the SQLite database file, used by the app and migrations at runtime.
DATABASE_PATH=./data/db.sqlite
# Path to the same file, read only by kysely-codegen (pnpm run db:codegen).
DATABASE_URL=./data/db.sqlite
